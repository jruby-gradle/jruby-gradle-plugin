language: groovy
sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer

install:
 - ./gradlew --no-daemon clean

script:
 - ./gradlew -S -i --no-daemon build assemble check gradleTest

jdk:
 - oraclejdk8

os:
 - linux

env: TERM=dumb

cache:
  directories:
    - $HOME/.gradle
