language: groovy
sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer

install:
 - ./gradlew clean --no-daemon

script:
 - ./gradlew -S -i build gradleTest --no-daemon

jdk:
 - oraclejdk8

os:
 - linux

env: TERM=dumb

cache:
  directories:
    - $HOME/.gradle
